valid_angle_range_deg: 60.0  # angular range considered "front" (±60° from center) — readings outside are ignored
min_valid_dist: 0.15         # minimum valid distance in meters — below this is noise or imminent collision
max_valid_dist: 5.0          # maximum valid distance in meters — above this the target is likely lost